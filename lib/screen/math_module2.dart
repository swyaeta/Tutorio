import 'package:flutter/material.dart';

class MathModule2 extends StatelessWidget {
  const MathModule2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFFF8FAFC),
      appBar: AppBar(
        title: const Text('Math Module 2'),
      ),
      body: const Center(
        child: Text(
          'Coming Soon !!',
          style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
        ),
      ),
    );
  }
}